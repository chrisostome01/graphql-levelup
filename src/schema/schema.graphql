### This file was generated by Nexus Schema
### Do not make changes to this file directly


type Card {
  description: String!
  id: Int!
  title: String!
  topic: Topic
}

type Mutation {
  deleteCard(id: Int!): Card!
  newTopic(topicName: String!): Topic!
  newUser(email: String!, password: String!): [User!]!
  post(description: String!, title: String!, topicId: Int!): Card!
  updateCard(description: String!, id: Int!, title: String!, topicId: Int!): Card!
  updateTopic(id: Int!, topicName: String!): Topic!
}

type Query {
  card: [Card!]!
  topic: [Topic!]!
  user: [User!]!
}

type Topic {
  cards: [Card!]!
  id: Int!
  topicName: String!
}

type User {
  Password: String!
  email: String!
  id: Int!
}