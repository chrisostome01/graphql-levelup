### This file was generated by Nexus Schema
### Do not make changes to this file directly


type AuthPayload {
  token: String!
  user: User!
}

type Card {
  description: String!
  id: Int!
  title: String!
  topic: Topic
}

type Mutation {
  deleteCard(id: Int!): Card!
  login(email: String!, password: String!): AuthPayload!
  newCard(description: String!, title: String!, topicId: Int!): Card!
  newTopic(topicName: String!): Topic!
  signup(email: String!, password: String!): AuthPayload!
  updateCard(description: String!, id: Int!, title: String!, topicId: Int!): Card!
  updateTopic(id: Int!, topicName: String!): Topic!
}

type Query {
  cards: [Card!]!
  topic: [Topic!]!
  user: [User!]!
}

type Topic {
  cards: [Card!]!
  id: Int!
  topicName: String!
}

type User {
  Password: String!
  email: String!
  id: Int!
}